# web-ele 接口对接核查报告

## 1. 核查说明

- 核查日期：2026-03-26
- 核查范围：`apps/web-ele`
- 核查原则：
  - 只检查 `web-ele` 内实际引用生成接口或生成类型的文件
  - 不修改 `apps/web-ele/src/api/core/*`、`apps/web-ele/src/api/types/*` 等脚本生成文件
  - 纯静态详情卡片文件与所属页面合并说明，不重复拆开
- 核查方法：
  - 以 `#/api/core`、`#/api/request`、`#/api/types` 的真实引用为入口收敛范围
  - 逐文件核对分页、查询字段、表单字段、详情回填、开关类更新、必填字段
  - 对非生成代码中的字段级错误直接修复

## 2. 本轮已直接修复的问题

### 2.1 页面管理分页页码仍按 1 基提交

- 文件：`apps/web-ele/src/views/app-manager/page-manager/index.vue`
- 问题：`appPagePageApi` 的 `pageIndex` 要求从 `0` 开始，但页面仍直接提交 `currentPage`
- 修复：改为统一走 `page.currentPage`

### 2.2 页面管理表单缺少 `title` 必填约束

- 文件：`apps/web-ele/src/views/app-manager/page-manager/model/shared.ts`
- 问题：`CreateAppPageDto.title` 为必填，但前端表单未校验
- 修复：为 `title` 补充 `rules: 'required'`

### 2.3 协议管理创建/编辑未稳定携带 `isPublished`

- 文件：`apps/web-ele/src/views/app-manager/agreement/index.vue`
- 问题：`CreateAgreementDto` / `UpdateAgreementDto` 中 `isPublished` 为必填，但当前表单未展示该字段，提交时可能丢失
- 修复：提交时保留编辑态详情中的 `isPublished`，新增时默认补 `false`

### 2.4 公告管理布尔必填字段缺少默认值

- 文件：
  - `apps/web-ele/src/views/app-manager/announcement/index.vue`
  - `apps/web-ele/src/views/app-manager/announcement/model/shared.ts`
- 问题：`CreateAnnouncementDto` 中 `isPinned`、`showAsPopup` 为必填，但表单新增时可空
- 修复：
  - 表单层给 `isPinned`、`showAsPopup` 增加默认值 `false`
  - 提交层再次兜底，避免空值透传

### 2.5 分类管理表单缺少 `isEnabled`，`sortOrder` 未强制填写

- 文件：`apps/web-ele/src/views/content-manager/category-manager/model/shared.ts`
- 问题：
  - `CreateCategoryDto` / `UpdateCategoryDto` 的 `isEnabled` 为必填，但表单没有该字段
  - `sortOrder` 为必填，但表单未做必填约束
- 修复：
  - 补充 `isEnabled` 单选项，默认 `true`
  - `sortOrder` 补默认值 `0` 和必填校验

### 2.6 板块管理提交时缺少 `followersCount`

- 文件：`apps/web-ele/src/views/forum/sections/index.vue`
- 问题：`CreateForumSectionDto.followersCount` / `UpdateForumSectionDto.followersCount` 为必填，但表单未展示该字段
- 修复：提交时保留编辑详情中的 `followersCount`，新增时默认补 `0`

### 2.7 板块分组表单缺少 `maxModerators`

- 文件：`apps/web-ele/src/views/forum/sections/modules/model/sectionGroupShared.ts`
- 问题：`CreateForumSectionGroupDto.maxModerators` 为必填，但表单未提供
- 修复：新增“版主数量上限”字段，默认 `0`，并加必填校验

### 2.8 漫画/小说作品表单缺少多项必填入参兜底

- 文件：
  - `apps/web-ele/src/views/content-manager/comic-manager/core/index.vue`
  - `apps/web-ele/src/views/content-manager/novel-manager/core/index.vue`
- 问题：`CreateWorkDto` 必填的 `canComment`、`chapterPrice`、`isPublished`、`recommendWeight`、`tagIds`、`viewRule` 等字段，当前表单并不都显式提交
- 修复：
  - 新增提交层统一兜底
  - 编辑态优先保留详情接口返回值，避免误把隐藏字段重置为默认值
  - 新增态补合理默认值：`canComment=true`、`chapterPrice=0`、`isPublished=false`、`recommendWeight=0`、`tagIds=[]`、`viewRule=0`

### 2.9 积分规则 / 经验规则缺少 `totalLimit`

- 文件：
  - `apps/web-ele/src/views/user-growth/points/modules/model/shared.ts`
  - `apps/web-ele/src/views/user-growth/experience/modules/model/shared.ts`
- 问题：`CreateUserPointRuleDto.totalLimit`、`CreateUserExperienceRuleDto.totalLimit` 为必填，积分规则更新 DTO 也要求该字段，但表单未提供
- 修复：
  - 补充“总上限”字段
  - 默认 `0`（无限制）
  - 表格列同步展示并格式化为“无限制 / 数值”

### 2.10 等级规则表单缺少 4 个新增必填字段

- 文件：`apps/web-ele/src/views/user-growth/level-rules/modules/model/shared.ts`
- 问题：`CreateUserLevelRuleDto` 的 `loginDays`、`discount`、`workCollectionLimit`、`blacklistLimit` 为必填，但页面未接入
- 修复：
  - 补齐上述 4 个字段
  - 默认值分别为 `0 / '1' / 0 / 0`
  - 为避免列表突然膨胀，这 4 个字段在表格中先隐藏，仅用于表单提交和详情数据完整性

## 3. 逐文件核查结论

> 说明：以下仅列出实际调用生成接口或直接承载接口字段映射的业务文件；纯 `detail.ts` 展示文件已并入所属页面说明。

### 3.1 基础设施与认证

- `apps/web-ele/src/store/auth.ts`：已对接 `authKeyPublicApi`、`authLoginApi`、`authLogoutApi`、`systemUserProfileApi`，登录与登出链路正常，未发现字段错位。
- `apps/web-ele/src/hooks/useDict.ts`：通过 `dictionaryItemListApi` 间接取字典项，属于基础设施接入，字段映射正常。
- `apps/web-ele/src/views/_core/authentication/login.vue`：已对接 `authCaptchaApi`，验证码、账号、密码字段匹配当前登录链路。
- `apps/web-ele/src/views/_core/profile/base-setting.vue`：已对接 `systemUserProfileApi` 做资料读取，属于只读资料回显场景。
- `apps/web-ele/src/views/system-manager/server-status/index.vue`：已对接 `apiHealthApi`、`apiReadyApi`，为探活/就绪页，不涉及复杂字段映射。

### 3.2 APP 管理

- `apps/web-ele/src/views/app-manager/page-manager/index.vue`：已对接 `appPagePageApi`、`appPageDetailApi`、`appPageCreateApi`、`appPageUpdateApi`、`appPageDeleteApi`，本轮已修复分页 `pageIndex` 0 基提交。
- `apps/web-ele/src/views/app-manager/page-manager/model/shared.ts`：表单字段与 `CreateAppPageDto` / `UpdateAppPageDto` 已对齐，本轮补齐 `title` 必填。
- `apps/web-ele/src/views/app-manager/announcement/index.vue`：已对接公告分页、详情、创建、更新、删除、发布状态切换，以及 `appPagePageApi` 页面下拉；`dateTimeRange -> publishStartTime/publishEndTime` 映射正确。
- `apps/web-ele/src/views/app-manager/announcement/model/shared.ts`：公告类型、优先级、平台映射正确；本轮补齐 `isPinned`、`showAsPopup` 的新增默认值。
- `apps/web-ele/src/views/app-manager/agreement/index.vue`：已对接协议分页、详情、创建、更新、删除、发布状态切换；本轮补齐 `isPublished` 必填兜底。
- `apps/web-ele/src/views/app-manager/agreement/model/shared.ts`：`title`、`version`、`content` 为必填，`showInAuth`、`isForce` 默认值合理，整体与生成 DTO 一致。
- `apps/web-ele/src/views/app-manager/app-config/index.vue`：已对接 `appConfigActiveApi`、`appConfigUpdateApi`，提交前会与当前配置合并，`appName`、`enableMaintenanceMode`、`themeColor`、`version` 均已正确入参。

### 3.3 内容管理

- `apps/web-ele/src/views/content-manager/tag-manager/index.vue`：已对接标签分页、详情、创建、更新、删除、启用状态、排序交换，未发现参数错位。
- `apps/web-ele/src/views/content-manager/tag-manager/model/shared.ts`：`CreateTagDto.name` 必填已落实；其余为展示/扩展字段，正常。
- `apps/web-ele/src/views/content-manager/category-manager/index.vue`：已对接分类分页、详情、创建、更新、删除、启用状态、排序交换。
- `apps/web-ele/src/views/content-manager/category-manager/model/shared.ts`：本轮已补齐 `isEnabled`、`sortOrder` 必填链路；`contentType` 目前前端比后端更严格，属于可接受的前端约束。
- `apps/web-ele/src/views/content-manager/author-manager/index.vue`：已对接作者分页、详情、创建、更新、状态切换、推荐切换、删除。
- `apps/web-ele/src/views/content-manager/author-manager/model/shared.ts`：`name`、`gender` 必填正确；`type` 当前前端要求选择，后端类型为可选，属于前端业务增强约束。
- `apps/web-ele/src/views/content-manager/comic-manager/core/index.vue`：已对接漫画分页、详情、创建、更新、删除、状态/推荐/热门/新作切换；本轮已补齐隐藏必填字段兜底，并确保编辑态保留详情值。
- `apps/web-ele/src/views/content-manager/comic-manager/core/model/shared.ts`：作者、分类、地区、语言、查看规则等字段整体可用；`price` 字段未在当前生成 `CreateWorkDto` / `UpdateWorkDto` 中声明，已在“待确认项”中单列。
- `apps/web-ele/src/views/content-manager/novel-manager/core/index.vue`：已对接小说分页、详情、创建、更新、删除、状态/推荐/热门/新作切换；本轮已补齐隐藏必填字段兜底，并确保编辑态保留详情值。
- `apps/web-ele/src/views/content-manager/novel-manager/core/model/shared.ts`：字段结构与漫画页同源；同样存在 `price` 为前端遗留扩展字段、未在当前生成 DTO 中声明的问题。
- `apps/web-ele/src/views/content-manager/comic-manager/chapter/index.vue`：已对接章节分页、详情、创建、更新、删除、排序交换；`workId` / `workType` 在提交时补入，接入正确。
- `apps/web-ele/src/views/content-manager/comic-manager/chapter/model/form.ts`：`sortOrder`、`title`、`viewRule` 等必填字段已正确校验，`canComment` / `canDownload` / `isPreview` 默认值完整。
- `apps/web-ele/src/views/content-manager/comic-manager/chapter/content-manager.vue`：已对接章节内容列表、删除、移动、清空；上传仍走 `es-upload + upload-url`，未复用生成 `contentComicChapterContentUploadApi`。
- `apps/web-ele/src/views/content-manager/comic-manager/chapter/archive-import-panel.vue`：已对接压缩包导入确认、任务详情；预解析上传仍通过 `requestClient.upload` 直传地址，未接入生成 `contentComicChapterContentArchivePreviewApi`。
- `apps/web-ele/src/views/content-manager/comic-manager/third-party/index.vue`：已对接 `contentComicThirdPartySearchPageApi`、`contentComicThirdPartyDetailApi`，分页换算已正确处理；平台列表、章节列表、章节正文详情接口仍未接入。
- `apps/web-ele/src/views/content-manager/novel-manager/chapter/index.vue`：已对接章节分页、详情、创建、更新、删除、排序交换；`workId` / `workType` 在提交时补入，接入正确。
- `apps/web-ele/src/views/content-manager/novel-manager/chapter/model/form.ts`：章节核心字段已对接；但 `content` 字段不在当前生成 `CreateWorkChapterDto` / `UpdateWorkChapterDto` 中声明，当前正文仍由专门内容编辑链路处理。
- `apps/web-ele/src/views/content-manager/novel-manager/chapter/content-editor.vue`：已对接正文详情读取、正文清空、章节更新；正文上传仍走固定 `upload-url`，未复用生成 `contentNovelChapterContentUploadApi`。

### 3.4 论坛管理

- `apps/web-ele/src/views/forum/sections/index.vue`：已对接板块分页、详情、创建、更新、删除、启用状态切换，以及分组分页、详情、创建、更新、删除、拖拽排序；本轮补齐 `followersCount` 必填兜底。
- `apps/web-ele/src/views/forum/sections/modules/model/shared.ts`：`name`、`isEnabled`、`sortOrder`、`topicReviewPolicy` 必填已落实，审核策略枚举与当前生成类型一致。
- `apps/web-ele/src/views/forum/sections/modules/model/sectionGroupShared.ts`：本轮补齐 `maxModerators`，其余 `name`、`isEnabled`、`sortOrder` 已正确。
- `apps/web-ele/src/views/forum/tags/index.vue`：已对接标签分页、详情、创建、更新、删除，未发现参数错位。
- `apps/web-ele/src/views/forum/tags/model/shared.ts`：`name`、`sortOrder`、`isEnabled` 均已接入，字段层无异常。
- `apps/web-ele/src/views/forum/sensitive-word/index.vue`：已对接分页、创建、更新、删除、状态切换；分页 `pageIndex` 已按 0 基处理。
- `apps/web-ele/src/views/forum/sensitive-word/modules/model/shared.ts`：`word`、`type`、`level`、`matchMode`、`isEnabled` 均已对齐；敏感词等级和匹配模式枚举使用正确。
- `apps/web-ele/src/views/forum/topic/index.vue`：已对接分页、详情、创建、更新、删除、审核、置顶/精华/锁定/隐藏，以及标签分配/解绑；`selectedUserIds -> userId` 映射正确，`dateRange -> startDate/endDate` 映射正确。
- `apps/web-ele/src/views/forum/topic/model/shared.ts`：创建表单必填 `sectionId`、`title`、`content` 已正确，审核表单 `auditStatus` 也已对齐。
- `apps/web-ele/src/views/forum/moderators/index.vue`：已对接分页、创建、更新、删除、分配板块；`selectedUserIds -> userId`、`sectionIds`、`groupId` 归一化逻辑正确。
- `apps/web-ele/src/views/forum/moderators/model/shared.ts`：版主角色、启用状态、板块/分组选项均已对接，表单必填逻辑正确。
- `apps/web-ele/src/views/forum/moderator-application/index.vue`：已对接分页、详情、审核、删除；`dateRange -> startDate/endDate` 映射正确。
- `apps/web-ele/src/views/forum/moderator-application/model/shared.ts`：板块选择和筛选字段正常。
- `apps/web-ele/src/views/forum/sensitive-word-statistics/index.vue`：已对接 `forumSensitiveWordStatsFullApi`，属于只读统计视图。

### 3.5 用户成长与用户管理

- `apps/web-ele/src/views/user-growth/points/index.vue`：已对接积分规则分页、详情、创建、更新；状态切换通过更新接口完成，字段完整。
- `apps/web-ele/src/views/user-growth/points/modules/model/shared.ts`：本轮补齐 `totalLimit`，`type`、`points`、`dailyLimit`、`isEnabled` 已对齐。
- `apps/web-ele/src/views/user-growth/experience/index.vue`：已对接经验规则分页、详情、创建、更新、删除；字段映射正常。
- `apps/web-ele/src/views/user-growth/experience/modules/model/shared.ts`：本轮补齐 `totalLimit`，`type`、`experience`、`dailyLimit`、`isEnabled` 已对齐。
- `apps/web-ele/src/views/user-growth/level-rules/index.vue`：已对接等级规则分页、详情、创建、更新、删除。
- `apps/web-ele/src/views/user-growth/level-rules/modules/model/shared.ts`：本轮补齐 `loginDays`、`discount`、`workCollectionLimit`、`blacklistLimit`，现已满足当前 `CreateUserLevelRuleDto` 必填要求。
- `apps/web-ele/src/views/user-growth/badges/index.vue`：已对接徽章分页、详情、创建、更新、删除、状态切换、用户分配/撤销、用户分页；接入完整。
- `apps/web-ele/src/views/user-growth/badges/modules/model/shared.ts`：`icon`、`name`、`type`、`sortOrder`、`isEnabled`、`description` 均已对齐。
- `apps/web-ele/src/views/user-manager/profile/index.vue`：已对接用户分页、详情、创建、编辑、恢复、删除、状态变更、启用切换、密码重置；查询 `dateRange -> startDate/endDate` 映射正确。
- `apps/web-ele/src/views/user-manager/profile/model/shared.ts`：用户创建/编辑/状态表单字段与当前管理接口匹配，未发现必填缺口。
- `apps/web-ele/src/views/user-manager/profile/components/user-operation-modal.vue`：已对接积分/经验发放与扣减、勋章授予与撤销、明细分页与统计，字段链路完整。

### 3.6 系统管理

- `apps/web-ele/src/views/system-manager/profile/index.vue`：已对接资料读取、资料更新、密码修改、登录历史分页；字段映射正常。
- `apps/web-ele/src/views/system-manager/profile/model/shared.ts`：资料编辑与密码变更必填项正常。
- `apps/web-ele/src/views/system-manager/user-manager/index.vue`：已对接管理员分页、详情、创建、更新、启停、密码重置、解锁；状态切换逻辑正确。
- `apps/web-ele/src/views/system-manager/user-manager/model/shared.ts`：创建与编辑表单字段与 `SystemUserCreateRequest` / `UpdateUserDto` 对齐，`password` 与 `confirmPassword` 仅在新增时保留，符合预期。
- `apps/web-ele/src/views/system-manager/data-dictionary/index.vue`：已对接字典分页、详情、创建、更新、删除、状态切换；手写分页已正确处理 0 基页码。
- `apps/web-ele/src/views/system-manager/data-dictionary/item.vue`：已对接字典项分页、创建、更新、删除、状态切换、排序交换；字典项 `dictionaryCode` 由当前字典上下文注入。
- `apps/web-ele/src/views/system-manager/data-dictionary/model/shared.ts`：字典与字典项表单字段基本完整，未发现新的字段错位。
- `apps/web-ele/src/views/system-manager/tasks/index.vue`：已对接任务分页、详情、创建、更新、删除、状态切换；`dateRange -> startDate/endDate` 查询映射正确。
- `apps/web-ele/src/views/system-manager/tasks/model/shared.ts`：`claimMode`、`completeMode`、`priority`、`status`、`targetCount` 等核心字段均已接入。
- `apps/web-ele/src/views/system-manager/tasks/task-assignment-modal.vue`：已对接 `taskAssignmentPageApi`，查询条件映射正常。
- `apps/web-ele/src/views/system-manager/log-manager/login-log/index.vue`：已对接 `auditPageApi`，`dateRange -> startDate/endDate` 映射正确。
- `apps/web-ele/src/views/system-manager/log-manager/login-log/model/shared.ts`：查询字段与日志分页接口保持一致。
- `apps/web-ele/src/views/system-manager/log-manager/operation-log/index.vue`：已对接 `auditPageApi`，查询字段包括 `username`、`ip`、`method`、`path`、`apiType`、`actionType`、`isSuccess`，映射正常。
- `apps/web-ele/src/views/system-manager/log-manager/operation-log/model/shared.ts`：查询 schema 与接口字段保持一致。
- `apps/web-ele/src/views/system-manager/system-config/index.vue`：已对接 `systemConfigApi`、`systemUpdateApi`，读写字段未发现错位。

## 4. 已导出但尚未接入业务页面的接口

### 4.1 APP / 用户

- `appPageDetailCodeApi`
- `appUsersRebuildFollowCountApi`
- `appUsersRebuildFollowCountAllApi`

### 4.2 内容管理

- `contentComicChapterContentUploadApi`
- `contentComicChapterContentUpdateApi`
- `contentComicChapterContentArchivePreviewApi`
- `contentComicThirdPartyPlatformListApi`
- `contentComicThirdPartyChapterListApi`
- `contentComicThirdPartyChapterContentDetailApi`
- `contentNovelChapterContentUploadApi`
- `contentAuthorRebuildFollowCountApi`
- `contentAuthorRebuildFollowCountAllApi`
- `contentAuthorRebuildWorkCountApi`
- `contentAuthorRebuildWorkCountAllApi`

### 4.3 论坛

- `forumSearchPageApi`
- `forumSensitiveWordDetectApi`
- `forumSensitiveWordStatsApi`
- `forumSensitiveWordReplaceApi`
- `forumSensitiveWordDetectHighestLevelApi`
- `forumSensitiveWordDetectStatusApi`
- `forumSensitiveWordCountApi`
- `forumSectionsTreeApi`
- `forumSectionsRebuildFollowCountApi`
- `forumSectionsRebuildFollowCountAllApi`
- `forumSectionsSwapSortOrderApi`
- `forumSectionGroupsUpdateEnabledApi`
- `forumTagsPopularListApi`
- `forumTagsEnabledListApi`

### 4.4 用户成长

- `growthExperienceRulesGrantApi`
- `growthExperienceRulesRecordPageApi`
- `growthExperienceRulesRecordDetailApi`
- `growthExperienceRulesStatsApi`
- `growthLevelRulesUserDetailApi`
- `growthLevelRulesPermissionCheckApi`
- `growthLevelRulesStatsApi`
- `growthBadgesStatsApi`

### 4.5 消息与上传

- `messageMonitorOutboxSummaryApi`
- `messageMonitorWsSummaryApi`
- `uploadFileUploadApi`

## 5. 间接接入但非页面直连

- `authTokenRefreshApi`：由 `apps/web-ele/src/api/request.ts` 负责 token 刷新，不属于单页面直连。
- `dictionaryItemListApi`：由 `apps/web-ele/src/hooks/useDict.ts` 间接调用，属于基础字典能力。
- 上传链路：当前多个页面仍通过 `es-upload`、`useUpload`、`requestClient.upload` 或固定 `upload-url` 直连上传地址，并未统一复用生成上传接口。

## 6. 当前遗留风险 / 待确认项

- `apps/web-ele/src/views/content-manager/comic-manager/core/model/shared.ts` 与 `apps/web-ele/src/views/content-manager/novel-manager/core/model/shared.ts` 中的 `price` 字段，当前生成 `CreateWorkDto` / `UpdateWorkDto` 未声明该字段。
  - 现状：页面仍保留该输入项。
  - 建议：后续确认后端 OpenAPI 是否遗漏 `price`，若已废弃则应删除前端字段。
- `apps/web-ele/src/views/content-manager/novel-manager/chapter/model/form.ts` 中的 `content` 字段，不在当前生成 `CreateWorkChapterDto` / `UpdateWorkChapterDto` 内。
  - 现状：正文仍通过 `content-editor.vue` 的专门正文链路维护。
  - 建议：后续把“章节元信息”和“章节正文”在 UI 上进一步拆清，避免误导。
- `apps/web-ele/src/views/content-manager/comic-manager/chapter/archive-import-panel.vue`、`apps/web-ele/src/views/content-manager/comic-manager/chapter/content-manager.vue`、`apps/web-ele/src/views/content-manager/novel-manager/chapter/content-editor.vue` 的上传仍非生成接口调用。
  - 现状：上传增强能力仍是“局部直连上传地址”
  - 建议：后续如果要统一接口治理，优先把上传和预解析链路改回生成 API 封装

## 7. 验证结果

- 已执行：`pnpm exec eslint --fix <本轮变更文件>`
- 已执行：`pnpm exec vue-tsc --noEmit -p apps/web-ele/tsconfig.json`
- 结果：通过

## 8. 结论

- `apps/web-ele` 当前核心 CRUD 页面大部分已经完成接口对接。
- 本轮新发现并修复的重点问题，主要集中在“生成 DTO 新增必填字段，但页面表单或提交层未同步”的场景。
- 当前未接入接口仍主要集中在统计、重建、检测、上传增强能力、第三方章节能力。
- 报告已落地到本地：`docs/接口对接核查报告.md`
