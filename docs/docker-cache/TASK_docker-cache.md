# Docker 构建缓存优化任务分解

## 任务列表

### 1. 修改工作流文件

- **目标**: 更新 `.gitea/workflows/web-ele-docker.yml`
- **内容**: 添加 `cache-from` 和 `cache-to` 配置。
- **验证**: 提交代码后触发 CI，观察日志。

### 2. 验证构建结果

- **目标**: 确认缓存生效。
- **方法**: 需要至少运行两次 CI。第一次填充缓存，第二次验证缓存命中。由于我无法直接触发 CI 多次并等待，我将完成配置修改并通知用户验证。
