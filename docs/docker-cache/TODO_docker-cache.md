# 待办事项清单

## 待验证
- [ ] **验证缓存生效**: 请在合并代码后，手动触发两次 CI 构建。检查第二次构建的日志，确认 Docker 层是否显示 `CACHED`，以及整体耗时是否减少。

## 建议
- 如果发现 `type=gha` 在您的 Gitea 环境中不工作（例如报错或无效果），可能需要检查 Gitea Runner 的配置，或者尝试切换到 `type=local` 配合 `actions/cache` 步骤（这需要更复杂的配置）。
