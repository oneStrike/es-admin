# 后续优化建议 (TODO)

## 1. 监控与调优

- [ ] **观察构建时间**: 限制并发可能会稍微延长构建时间。如果机器性能允许（如内存升级后），可以适当调高并发数（如 3 或 4）。
- [ ] **Docker 缓存优化**: 目前 Docker 构建时复制了整个上下文。未来可以考虑使用 `turbo prune` 提取最小构建上下文，进一步减少 I/O 和镜像体积。

## 2. CI/CD 环境

- [ ] **独立 Runner 配置**: 如果是在 CI 环境运行，建议检查 CI Runner 的内存配置，确保与 `NODE_OPTIONS` 匹配。
